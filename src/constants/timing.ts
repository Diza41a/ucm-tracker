/** Delay before persisting autosaved form fields after the last edit. */
export const SAVE_DEBOUNCE_MS = 1000;

/** Delay before persisting win-day text after the last edit. */
export const WIN_SAVE_DEBOUNCE_MS = 1500;
